<Window x:Class="FactoryHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:FactoryHelper.Converters"
        mc:Ignorable="d"
        Title="Yovo ADB Tools" Height="650" Width="1050"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        FontFamily="Microsoft YaHei">

    <Window.Resources>
        <conv:NullToCollapsedConverter x:Key="NullToCollapsed"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ======== 顶部平台栏 ======== -->
        <Border Grid.Row="0" Background="#1e1e1e" Padding="10,6"
                BorderBrush="#333" BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="Yovo ADB Tools" FontSize="14" FontWeight="Bold"
                           Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ADB: " FontSize="12" Foreground="#aaa" VerticalAlignment="Center"/>
                    <TextBlock Name="TxtAdbStatus" Text="{Binding AdbStatus}" FontSize="12"
                               FontWeight="Bold" Foreground="#4caf50" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 模块导航 -->
                <ListBox ItemsSource="{Binding Modules}"
                         SelectedItem="{Binding ActiveModule}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,0,4,0"/>
                            <Setter Property="Padding" Value="12,4"/>
                            <Setter Property="Foreground" Value="#ddd"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#0078d4"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- ======== 模块内容区 ======== -->
        <ContentControl Grid.Row="1" Content="{Binding ActiveView}"/>

        <!-- ======== 底部状态栏 ======== -->
        <Border Grid.Row="2" Background="#f0f0f0" Padding="10,4" BorderBrush="#d0d0d0" BorderThickness="0,1,0,0">
            <TextBlock Text="Yovo ADB Tools v1.0" FontSize="11" Foreground="#888"/>
        </Border>
    </Grid>
</Window>