<Window x:Class="FactoryHelper.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="参数输入" Height="Auto" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Microsoft YaHei"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Name="TxtTitle" Grid.Row="0" FontSize="14" FontWeight="Bold"
                   Margin="0,0,0,4"/>
        <TextBlock Name="TxtCommand" Grid.Row="1" FontSize="11" Foreground="#666"
                   Margin="0,0,0,12" TextWrapping="Wrap"/>

        <StackPanel Name="InputPanel" Grid.Row="2"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="确定" Width="72" Height="30" IsDefault="True"
                    Click="OnOkClick" Margin="0,0,8,0"
                    Background="#0078d4" Foreground="White" BorderThickness="0"/>
            <Button Content="取消" Width="72" Height="30" IsCancel="True"
                    Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>