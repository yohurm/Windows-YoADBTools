[package]
name = "fake-adb"
description = "Yohu ADB Tools — 脚本化假 adb.exe（core 集成测试 fixture，不发布）"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
